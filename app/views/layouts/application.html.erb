<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <ul>
        <li><%= link_to "PrivateEvents", root_path, class: "logo" %></li>
        <ul class="navbar-right">
          <li><%= link_to "Create Event", new_event_path, class: "nav-button"%></li>
          <% if user_signed_in? %>
            <li><%= link_to "Account", user_path, class: "nav-button" %></li>
            <li><%= link_to "Log Out", destroy_user_session_path, class: "nav-button"%>
          <% else %>
            <li><%= link_to "Log In", new_user_session_path, class: "nav-button"%></li>
            <li><%= link_to "Sign Up", new_user_registration_path, class: "nav-button"%></li>
          <% end %>
        </ul>
      </ul>
    </nav>
    <%= yield %>
  </body>
</html>
